//
//  sequencer.cpp
//  REmap
//
//  Created by Afnan Enayet on 8/15/15.
//  Copyright (c) 2015 Afnan Enayet. All rights reserved.
//

#include "sequencer.h"

std::vector < std::vector < int > > findCutLocations (std::vector <std::string> keyVector, std::vector < std::vector <int> > fragmentSizes) {
    std::vector <int> temp_row; //just creating a temporary vector so we can return it and have the project compile without error
    std::vector < std::vector <int> > temp_return;
    temp_row.push_back(0);
    temp_return.push_back(temp_row);
    
       //TODO: figure out logic sequence function
    return temp_return; //deal with it
}
